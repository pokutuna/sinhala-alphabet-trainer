# Supply-chain attack mitigation:
# Do not install package versions published within the last 14 days.
# This gives time for malicious releases to be detected and yanked.
# Value is in days. Requires npm >= 11.10.0 (ignored by older npm).
# Kept in sync with the `cooldown` setting in .github/dependabot.yml.
min-release-age=14
